# Production dependencies — used by Railway deploy.
# For local development with test/lint tools: pip install -r requirements/development.txt

# Core Django
Django==6.0.2
django-htmx==1.27.0
# django-smartbase-admin==1.1.1 — installed via --no-deps in railpack.json build step
# (its django<6.0 upper bound is overly cautious; works fine with 6.0.2)
# Transitive deps listed explicitly because --no-deps skips them:
django-admin-inline-paginator>=0.4.0,<0.5.0
django-ckeditor>=6.7.1,<7.0.0
django-filer>=3.1.1,<4.0.0
django-nested-admin>=4.1.1,<5.0.0
setuptools>=67.0.0,<68.0.0
xlsxwriter>=3.2.0,<4.0.0
easy-thumbnails>=2.10
django-widget-tweaks>=1.5
django-anymail[resend]==14.0
django-storages[s3]==1.14.6
python-dotenv==1.2.1

# Database
dj-database-url==3.1.2
psycopg2-binary==2.9.11

# File handling
boto3==1.42.54
pillow==12.1.1
PyMuPDF==1.26.5
openpyxl==3.1.5

# Security
django-ratelimit==4.1.0

# Component library + forms
django-cotton>=1.0.0
django-template-partials>=24.4
django-tailwind>=3.8.0
django-crispy-forms>=2.3

# Static files
whitenoise==6.11.0

# WSGI server
gunicorn==25.1.0
