{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": [
        "...",
        "python -m pip install --no-deps django-smartbase-admin==1.1.1"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm --prefix theme/static_src install && python manage.py tailwind build && python manage.py collectstatic --noinput && python manage.py migrate --noinput && python manage.py ensure_superuser && gunicorn config.wsgi --bind 0.0.0.0:$PORT --workers 3"
  }
}
